server:
  port: 8097
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: company-service
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka